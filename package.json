{
  "name": "nexora",
  "private": true,
  "version": "0.1.0",
  "packageManager": "yarn@1.22.22",
  "scripts": {
    "dev": "pnpm --parallel --filter @nexora/api --filter @nexora/web dev",
    "build": "corepack pnpm install --no-frozen-lockfile && corepack pnpm --filter @nexora/web build",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck"
  },
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  }
}